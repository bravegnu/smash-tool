Minor Tasks:

  * Calulate the progress bar width from COLWIDTH environment variable.
  * Add eavesdrop functionality to command line interface.
  * Handler serial exception correctly.
  * GUI should ask if all blocks used by the file is to be erased.

Major Tasks:

  * Make command line interface more powerful. Command line interface
    should have the following sub-commands.
    - prog
    - erase
    - dump
	
Bugs:

 * When an operation is in progress (say dump or program) another
   operation should not be started.

